export enum Endpoint {
  Everything = 'everything',
  Sources = 'sources',
  Not = '',
}

export enum HttpMetods {
  GET = 'GET',
  POST = 'POST',
}

export enum HttpStatusCode {
  Unauthorized = 401,
  NotFound = 404,
}
